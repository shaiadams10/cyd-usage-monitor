# Local model artifacts

Download the trainer's outputs here. The generated `.tflite`, `.onnx`, audio,
and dataset files are intentionally ignored by Git.

The deployable file must be named `hey_burden.tflite`. The ONNX output is useful
for non-Home-Assistant runtimes, but the Home Assistant openWakeWord app loads the
TensorFlow Lite file.

