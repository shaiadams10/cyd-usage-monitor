# Changelog

## Unreleased

- Replaced deployment-specific example addresses and lamp entity IDs with
  documentation-only addresses and configurable entity macros. Real values
  remain in the ignored `include/secrets.h` file.
