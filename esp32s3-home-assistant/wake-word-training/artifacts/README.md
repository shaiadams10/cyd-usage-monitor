# Local microWakeWord artifacts

`hey_burden.tflite` and `hey_burden.json` are generated locally and intentionally
ignored. The training data has mixed upstream licenses, so the resulting model
is for this operator's personal deployment and is not published by default.

